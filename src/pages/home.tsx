import Navbar from "../components/Navbar";

const HomePage = () => {
  return (
    <section className={`min-h-screen bg-cover bg-home-desktop`}>
      <Navbar />
    </section>
  );
}
 
export default HomePage;
